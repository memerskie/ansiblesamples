pipeline {
  agent { label 'linux' }
  stages {
    stage('Hello') 
        ansible-galaxy collection install -r requirements.yml
        ansible-playbook -i inventory/mariadb.hosts playbooks/mariadb.yml
    }
  }

